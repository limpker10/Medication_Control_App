export default class Medicamento {
  constructor(nombre, dosis, fechaHora, periodo) {
    this.nombre_medicamento = nombre;
    this.dosis = dosis;
    this.fecha_hora = fechaHora;
    this.periodo = periodo;
  }
}
